<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TestMauiFaisabilite_KronoGeo.TestPhotoVideo"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewmodels="clr-namespace:TestMauiFaisabilite_KronoGeo.ViewModels"
             x:DataType="viewmodels:TestPhotoVideoViewModel"
             Title="TestPhotoVideo">
    
    <VerticalStackLayout>
        <Grid ColumnDefinitions="*,*,*" RowDefinitions="*,30,30">
                <toolkit:CameraView
            x:Name="Camera" 
            Grid.ColumnSpan="3" 
            Grid.Row="0"
            SelectedCamera="{Binding SelectedCamera}"
            ZoomFactor="{Binding CurrentZoom}"
            CameraFlashMode="{Binding FlashMode}" />

                <Slider 
            Grid.Column="0"
            Grid.Row="1"
            Value="{Binding CurrentZoom}"
            Maximum="{Binding SelectedCamera.MaximumZoomFactor, FallbackValue=1}"
            Minimum="{Binding SelectedCamera.MinimumZoomFactor, FallbackValue=1}"/>

                <Picker 
            Grid.Column="1"
            Grid.Row="1"
            Title="Flash"
            IsVisible="{Binding Path=SelectedCamera.IsFlashSupported, FallbackValue=false}"
            ItemsSource="{Binding FlashModes}"
            SelectedItem="{Binding FlashMode}" />

            <Picker 
            Grid.Column="2"
            Grid.Row="1"
            Title="Available Resolutions"
            ItemsSource="{Binding SelectedCamera.SupportedResolutions}"
            SelectedItem="{Binding SelectedResolution}" />
        </Grid>


        <HorizontalStackLayout>
            <ImageButton Command="{Binding CaptureImageCommand, Source={x:Reference Camera}}">
                <ImageButton.Source>
                    <FontImageSource FontFamily="MaterialIcons-Regular" Glyph="&#xe412;"/>
                </ImageButton.Source>
            </ImageButton>

        </HorizontalStackLayout>

    </VerticalStackLayout>
    
</ContentPage>